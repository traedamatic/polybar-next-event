{
  "name": "polybar-next-event",
  "version": "0.1.0",
  "description": "Polybar module that shows the next calendar event with color-coded urgency",
  "type": "module",
  "scripts": {
    "dev": "bun --hot src/index.ts",
    "test": "bun test",
    "build": "bun build src/polybar/output.ts --outdir ./dist/polybar --target bun"
  },
  "license": "MIT",
  "devDependencies": {
    "@types/bun": "^1.3.13"
  },
  "dependencies": {
    "ical.js": "^2.2.1",
    "tsdav": "^2.1.8",
    "webview-bun": "^2.4.0"
  }
}
