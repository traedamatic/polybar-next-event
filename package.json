{
  "name": "polybar-next-event",
  "version": "0.1.0",
  "description": "Polybar module that shows the next calendar event with color-coded urgency",
  "type": "module",
  "scripts": {
    "dev": "bun --hot src/index.ts",
    "test": "bun test",
    "build": "bun build src/index.ts --outdir ./dist --target bun"
  },
  "license": "MIT",
  "devDependencies": {
    "@types/bun": "^1.3.13"
  }
}
